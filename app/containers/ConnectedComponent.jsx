import { connect } from 'react-redux';

const mapStateToProps = (state, ownProps) => ({});

const mapDispatchToProps = dispatch => ({});

/**
 * @desc Connected component.
 */
export default connect(mapStateToProps, mapDispatchToProps)();
